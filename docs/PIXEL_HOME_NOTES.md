## Scope note

The pixel-home branch is intentionally visual. It reuses the existing Quest dashboard DOM, clock, task/anchor controls, XP calculations, Voyage Adjustment and backend. No normalized-database or sync code was changed.
