# Pixel home test checklist

On iPhone preview, verify:

1. Login still works and opens the dashboard.
2. The farm artwork loads in the hero.
3. The existing daily clock still shows scheduled task/anchor markers and updates time.
4. Daily XP values still update when a task/anchor is completed.
5. Anchor completion still works from the home summary.
6. Today's quest completion still opens the existing completion-time flow where applicable.
7. Stop Day opens the existing voyage adjustment flow; Safe Harbor behavior is unchanged.
8. Bottom navigation remains usable and does not cover the last visible control.
9. Offline/reconnect behavior still works because no sync logic changed in this stage.
